# Deprecated images

These files had zero references anywhere in the repo's `.html`, `.css`, `.js`,
`.json`, and `.md` files as of 2026-09-08. Everything else that was initially
flagged unreferenced was reviewed and moved back into `Images/` for planned use.
